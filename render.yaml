services:
  - type: web
    name: ai-plant-disease-scanner
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --workers 1 --threads 1 --timeout 120 app:app
    autoDeploy: true